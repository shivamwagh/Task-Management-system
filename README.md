# Task-Management-system
A Spring Boot–based full-stack application that allows users to manage daily tasks efficiently with authentication, role-based access, and CRUD operations. The system supports Admin and User roles, with secure JWT authentication and PostgreSQL as the database.
